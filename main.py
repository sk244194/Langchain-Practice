# Always create a virtual enviroment before working on a project to install certain version of libraries needed for that project
# uv venv

# for dependencies you could only use requirements.txt to install it directly using uv add -r require... or pip install -r require...

# Using groq api and gemini api -> Free


# So i have also forked the github(Langchain) from which i am studying so read that as well

